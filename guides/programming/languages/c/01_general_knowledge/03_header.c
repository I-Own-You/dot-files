// 1. a "header" usually contains declalrations, macros, types, .etc. but IS NOT itself a library,
//    so "header" is needed to look for info which is needed in your file,
//    and that info is searched inside a library during "linking" phase.
#include <stdio.h> // stdio.h is a header
