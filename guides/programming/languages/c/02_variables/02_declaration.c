// 1. before you use a variable you must declare it.
// 2. to declare a variable you put TYPE first and the NAME of it
// 3. if several variable are of same type then they can be aligned in one line with a comma between

int main() {
    // 1. variable of type int declared
    int height;
    // 1. variable of type float declared
    float profit;
}
