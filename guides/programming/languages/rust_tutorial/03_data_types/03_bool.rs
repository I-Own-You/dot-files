fn main() {
    let t = true;
    let f = false;

    let ff: bool = false;

    // 1. bool type cannot participate in numeric operations: 1 + t is error,
    //    the main usage is within if clause
}
