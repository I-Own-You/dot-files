DROP TABLE IF EXISTS mytable; -- be cautious, it will change the schema of the db and remove the table from it also
                              -- if some tables depend on this table, it will throw an error
