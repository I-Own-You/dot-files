<!-- svelte provides some special elements you can use for easier development such as:
     1. <svelte:window>      window object of the page
     2. <svelte:document>    document object
     3. <svelte:body>        body tag on page
     4. <svelte:head>        head tag on page
     5. <svelte:element>     useful to choose what to render without if\else if\else blocks
     6. <svelte:boundary>    wrapper for components which produces errors

    look into docs for more info
-->
